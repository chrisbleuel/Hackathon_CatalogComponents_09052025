import React from 'react';
import { useIsMobile } from '@/hooks/use-mobile';
import { Menu } from 'lucide-react';
export const Header = () => {
  const isMobile = useIsMobile();
  return <div className="justify-center items-stretch flex w-full flex-col bg-white">
      
    </div>;
};